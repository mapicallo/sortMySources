{
  "name": "sortmysources-monorepo",
  "private": true,
  "description": "SortMySources — organize sources into maps (PWA + extension)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build -w @sortmysources/core && npm run build -w sortmysources-pwa && npm run build -w sortmysources-extension",
    "dev:pwa": "npm run dev -w sortmysources-pwa",
    "dev:ext": "npm run dev -w sortmysources-extension",
    "pack:ext": "npm run pack -w sortmysources-extension"
  }
}
